type Author {
    id: ID!
    name: String!
    age: Int
    books: [Book]
}

# Root
type Query {
    findAllAuthors: [Author]!
}

# Root
type Mutation {
    createAuthor(name: String!, age: Int): Author!
}